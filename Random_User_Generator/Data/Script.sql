CREATE DATABASE random_user_generator;

CREATE TABLE USERS(
                      Id int PRIMARY KEY,
                      Gender VARCHAR(20),
                      Name VARCHAR(100),
                      Email VARCHAR(200)
);